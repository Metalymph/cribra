[package]
name = "silens-scan"
version = "0.1.0"
edition = "2024"

[features]
default = []
parallel = ["dep:rayon"]

[dependencies]
aho-corasick = "1.1.4"
rayon = { version = "1.12.0", optional = true }
regex = "1.13.1"

[[bench]]
name = "scan"
harness = false

[dev-dependencies]
criterion = "0.8.2"
